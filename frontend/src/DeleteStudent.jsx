import React from 'react';


class DeleteStudent extends React.Component {     //  有render的必定是class component
  state = { student : null }

// HandleStudentDelete = async(id) => {
//   await axios.delete(`http://localhost:5000/products/${id}`)
}
